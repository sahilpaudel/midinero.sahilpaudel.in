{
  "name": "midinero",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Private, on-device wealth tracker — assets and liabilities in one place.",
  "homepage": "https://midinero.sahilpaudel.in",
  "scripts": {
    "dev": "vite",
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && vite build",
    "predeploy": "yarn run build && cp CNAME build/CNAME",
    "deploy": "gh-pages -d build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "vite": "^5.4.10"
  }
}
